---
title: Docker Compose
description: "Run a TBMQ cluster with Docker Compose: create the Docker volumes, install the database, start the services behind HAProxy, then upgrade or enable MQTTS."
head:
  - tag: title
    content: TBMQ Cluster with Docker Compose
---

import PageContent from '@includes/docs/mqtt-broker/installation/cluster/docker-compose-setup.mdx'
import { Products } from '~/models/site.models'

<PageContent product={Products.TBMQ_PE}/>
