---
title: Docker (Linux & macOS)
description: "Install a standalone TBMQ instance with Docker on Linux or macOS: run the install script, view logs, stop and start the broker, upgrade and enable MQTTS."
---

import PageContent from '@includes/docs/mqtt-broker/installation/docker.mdx'
import { Products } from '~/models/site.models'

<PageContent product={Products.TBMQ}/>
