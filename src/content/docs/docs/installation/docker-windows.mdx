---
title: Docker (Windows)
description: "Install a standalone TBMQ instance with Docker Desktop on Windows: run the PowerShell install script, manage the containers, upgrade and enable MQTTS."
head:
  - tag: title
    content: Install TBMQ with Docker on Windows
---

import PageContent from '@includes/docs/mqtt-broker/installation/docker-windows.mdx'
import { Products } from '~/models/site.models'

<PageContent product={Products.TBMQ}/>
