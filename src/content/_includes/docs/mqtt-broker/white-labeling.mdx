import ImageGallery from '~/components/ImageGallery.astro';
import DocLink from '@components/DocLink.astro';

White labeling lets you tailor your TBMQ instance to match your brand — particularly useful for companies delivering IoT solutions to their customers.
Configure your company name, upload your logo, and choose color palettes.

The **White labeling** page has three tabs: **General** for the management UI, **Login** for the login page, and
<DocLink product={props.product} path="image-gallery">Image gallery</DocLink> for the logo and icon assets both tabs draw from.
Editing these settings requires the <DocLink product={props.product} path="security/rbac">Administrator</DocLink> role — a Viewer sees them read-only.

## Customize TBMQ web interface

To configure your **logo** and **color scheme**, go to the **White labeling** page and open the **General** tab.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-default.png',
    alt: 'White labeling page with default settings', caption: 'White labeling page with default settings',
  },
]} />

The tab provides the following settings:

- **Application title** — custom page title displayed in the browser tab. Up to 255 characters.
- **Website icon** — replace the default browser tab icon with your own.
- **Logo** — replace the default logo in the upper left corner with your company logo.
- **Logo height, px** — resize the logo.

For the **Website icon** and **Logo**, either **Browse from gallery** to pick an asset from the
<DocLink product={props.product} path="image-gallery">Image gallery</DocLink> — so the same file can be reused and updated in
one place — or **Set link** to reference an external image URL.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-title-logo-favicon.png',
    alt: 'Application title, favicon, and logo settings', caption: 'Application title, favicon, and logo settings',
  },
]} />

- **Primary palette** — customize the background and font colors by selecting a suggested UI design or modifying an existing one.
- **Accent palette** — customize the color for elements such as toggles.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/primary-accent-palette.png',
    alt: 'Primary and accent color palette settings', caption: 'Primary and accent color palette settings',
  },
]} />

- **Advanced CSS** — apply custom CSS to any element of the TBMQ interface. See [Advanced CSS](#advanced-css) below.
- **Hide connectivity dialog** and **Hide Getting Started page** — hide the check-connectivity dialog and the Getting Started page.
- **Show platform name and version** — when enabled, reveals the required **Platform Name** and **Platform version** fields
  (defaulting to `TBMQ` and the current broker version) and displays their values in the lower left corner.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/show-platform-name-and-version.png',
    alt: 'Platform name and version displayed in the lower left corner', caption: 'Platform name and version displayed in the lower left corner',
  },
]} />

Use **Preview** to try the result before saving, and **Reset to default** to discard all customizations. The customized interface:

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-custom.png',
    alt: 'Final look of the customized TBMQ interface',
    caption: 'Customized interface',
  },
]} />

### Advanced CSS

Use CSS to stylize any element of the TBMQ interface — backgrounds, icons, fonts, and more.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-advanced-css-1.png', alt: 'Click the Advanced CSS button', caption: 'Open Advanced CSS' },
  { src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-advanced-css-2.png', alt: 'Paste CSS code and click Save', caption: 'Enter CSS and save' },
  { src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-advanced-css-3.png', alt: 'Result: icons color changed to purple, active menu button changed to white', caption: 'Result' },
]} />

Example — customize icon color, scrollbar color, and active button color:

```css
/* Icon color */
a.mat-mdc-button.mat-mdc-button-base .mat-icon {
    color: #a60062;
}

/* Scrollbar color */
mat-toolbar::-webkit-scrollbar-thumb,
div::-webkit-scrollbar-thumb,
ng-component::-webkit-scrollbar-thumb {
    background-color: #c526a5 !important;
    background-image: linear-gradient(#e72c83, #a742c6);
    border-radius: 200px/300px !important;
    border: 0.1rem linear-gradient(#e72c83, #a742c6);
}

/* Active button color */
.mat-mdc-button.mat-mdc-button-base.tb-active {
    color: #a60062;
}
```

## Customize the login page

On the **Login** tab, configure the TBMQ login page:

- **Domain name** — select an already registered domain or create a new one inline. For details, see <DocLink product={props.product} path="security/domains/#domain-registration">Domain registration</DocLink>.
- **Base URL** — the absolute URL of the instance, used in generated links. Prefilled from the selected domain, for example `https://mqtt.mycompany.com`.
- **Prohibit to use hostname from the client request headers** — enable this in production, so the broker uses the configured **Base URL** instead of trusting the request headers.
- **Application title**, **Website icon**, and **Logo** — the same branding fields as on the **General** tab.
- **Primary palette** and **Accent palette** — the login page color scheme.
- **Dark foreground** — switch the login form text and icons to dark, for use with a light background.
- **Page background color** — the color behind the login form.
- **Show platform name and version** — as on the **General** tab, plus **Platform name and version position**, which places the values either under the logo or at the bottom of the login form.

Once done, save the changes.

<ImageGallery product={props.product} images={[
  { src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-login-1.png', alt: 'Login page configuration — domain, title, favicon, logo', caption: 'Domain, title, and logo settings' },
  { src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-login-2.png', alt: 'Login page configuration — color palettes and background', caption: 'Color settings' },
]} />

Use your custom domain to access the TBMQ login page and verify the result.

<ImageGallery product={props.product} images={[
  {
    src: '/src/assets/images/docs/mqtt-broker/white-labeling/white-labeling-login-3.png',
    alt: 'Customized TBMQ login page accessed via custom domain',
    caption: 'Customized login page',
  },
]} />
