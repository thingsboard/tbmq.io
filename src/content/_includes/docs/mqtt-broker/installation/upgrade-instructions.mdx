import InstallationCardGrid from '~/components/InstallationCardGrid.astro';
import Tabs    from '@components/Tabs.astro';
import TabItem from '@components/TabItem.astro';
import { Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';
import DocLink from '~/components/DocLink.astro';
import ConditionalHeading from '~/components/ConditionalHeading.astro';
import ShowFor from '~/components/ShowFor.astro';

<Tabs defaultTab="on-premises" urlParam="installationType">
  <TabItem label="On-premises" id="on-premises" icon="tabler:server-2">
    <h2>Standalone</h2>
    <InstallationCardGrid product={props.product} items={[
      { path: 'installation/docker#upgrading', title: 'Docker (Linux, macOS)', icon: '/src/assets/images/installation/docker-linux-mac.svg', accent: 'var(--sl-color-blue)' },
      { path: 'installation/docker-windows#upgrading', title: 'Docker (Windows)', icon: '/src/assets/images/installation/docker-windows.svg', accent: 'var(--sl-color-blue)' },
    ]} />
    <h2>Cluster</h2>
    <InstallationCardGrid product={props.product} items={[
      { path: 'installation/cluster/docker-compose-setup#upgrading', title: 'Docker Compose', icon: '/src/assets/images/installation/docker-compose.svg', accent: 'var(--sl-color-blue)' },
      { path: 'installation/cluster/minikube-cluster-setup#upgrading', title: 'Minikube', icon: '/src/assets/images/installation/minikube.svg', accent: 'var(--sl-color-green)' },
    ]} />
  </TabItem>
  <TabItem label="Cloud" id="cloud" icon="tabler:cloud">
    <InstallationCardGrid product={props.product} items={[
      { path: 'installation/cluster/aws-cluster-setup#upgrading', title: 'AWS', icon: '/src/assets/images/installation/aws.svg', accent: 'var(--sl-color-orange)' },
      { path: 'installation/cluster/azure-cluster-setup#upgrading', title: 'Azure', icon: '/src/assets/images/installation/azure.svg', accent: 'var(--color-product-trendz)' },
      { path: 'installation/cluster/gcp-cluster-setup#upgrading', title: 'Google Cloud Platform', icon: '/src/assets/images/installation/gcp-platform.svg', accent: 'var(--sl-color-red)' },
    ]} />
  </TabItem>
  <TabItem label="Helm" id="helm" icon="tabler:hexagon-letter-h">
    <InstallationCardGrid product={props.product} items={[
      { path: 'installation/cluster/helm-cluster-setup-minikube#upgrading', title: 'Minikube', icon: '/src/assets/images/installation/minikube.svg', accent: 'var(--sl-color-green)' },
      { path: 'installation/cluster/helm-cluster-setup-aws#upgrading', title: 'AWS EKS', icon: '/src/assets/images/installation/eks.svg', accent: 'var(--sl-color-orange)' },
    ]} />
    <InstallationCardGrid product={props.product} items={[
      { path: 'installation/cluster/helm-cluster-setup-azure#upgrading', title: 'Azure AKS', icon: '/src/assets/images/installation/azure-kubernetes.svg', accent: 'var(--color-product-trendz)' },
      { path: 'installation/cluster/helm-cluster-setup-gcp#upgrading', title: 'GCP GKE', icon: '/src/assets/images/installation/gcp-kubernetes.svg', accent: 'var(--sl-color-red)' },
    ]} />
  </TabItem>
</Tabs>

<ShowFor product={props.product} show={[Products.TBMQ_PE]}>
<ConditionalHeading level={2} id="version-history-pe" showFor="mqtt-broker-pe">Version history</ConditionalHeading>

Select a card above to open the upgrade guide for your deployment type, then follow its steps for the target version.
Each guide includes instructions for pulling the new image and running the upgrade script.

The table below lists all supported upgrade paths. Versions must be upgraded sequentially — you cannot skip versions.

| Target version | Upgrade from    | Notes                                                                                                                                                    |
| -------------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **2.3.0PE**    | 2.2.0PE         |                                                                                                                                                          |
| **2.3.0PE**    | TBMQ 2.3.0 (CE) | See [CE-to-PE migration](#upgrade-from-tbmq-to-tbmq-pe) below                                                                                            |
| **2.2.0PE**    | TBMQ 2.2.0 (CE) | See [CE-to-PE migration](#upgrade-from-tbmq-to-tbmq-pe) — [third-party changes apply](#third-party-component-updates-in-tbmq-pe-v220) |

<ConditionalHeading level={2} id="upgrade-from-tbmq-to-tbmq-pe" showFor="mqtt-broker-pe">Upgrade from TBMQ to TBMQ PE</ConditionalHeading>

You can migrate from TBMQ to TBMQ PE of the **same version**.
For example, TBMQ 2.3.0 can be upgraded to TBMQ PE 2.3.0.
Cross-version migration (e.g. TBMQ 2.2.0 to TBMQ PE 2.3.0) is **not supported** — upgrade TBMQ to the target version first, then migrate to TBMQ PE.

<Aside type="note">
  To upgrade to TBMQ PE 2.3.0, you must be running TBMQ 2.3.0 first. If you are still on an earlier version,
  <DocLink product={Products.TBMQ} path="installation/upgrade-instructions/">upgrade TBMQ</DocLink> to 2.3.0 before migrating to TBMQ PE.
</Aside>

<Aside type="tip">
  Starting from v2.3.0, both editions use identical third-party component versions. If you are migrating at v2.3.0 or later, no additional third-party changes are required — proceed directly with the CE-to-PE upgrade script.
</Aside>

<Aside type="tip">
  After the upgrade completes, clear your browser cache to ensure PE assets load correctly.
</Aside>

<ConditionalHeading level={3} id="third-party-component-updates-in-tbmq-pe-v220" showFor="mqtt-broker-pe">Third-party component updates (CE v2.2.0 → PE v2.2.0 only)</ConditionalHeading>

The following changes apply **only** when migrating from TBMQ v2.2.0 to TBMQ PE v2.2.0. They do not apply to v2.3.0 and later.

These updates follow the modernization plan outlined in the <a href="https://github.com/thingsboard/thingsboard-ce-k8s/blob/master/BITNAMI-IMAGE-MIGRATION.md#3-long-term-production-solution" target="_blank" rel="noopener noreferrer">Bitnami Image Migration</a> guide.

| Component            | TBMQ v2.2.0                  | TBMQ PE v2.2.0      | Notes                                                                                       |
| -------------------- | ---------------------------- | ------------------- | ------------------------------------------------------------------------------------------- |
| **PostgreSQL**       | `postgres:16`                | `postgres:17`       | Improved performance and resource efficiency. Existing data volumes remain compatible.      |
| **Kafka**            | `bitnamilegacy/kafka:3.7.0`  | `apache/kafka:4.0.0` | Migrated from Bitnami to the official Apache Kafka image. New volume paths are required.   |
| **Redis → Valkey** | `bitnamilegacy/redis:7.2.5`  | `valkey/valkey:8.0` | Migrated from Bitnami Redis to Valkey. A new volume `tbmq-valkey-data` is introduced.       |

Before starting, create full backups of your environment — including the PostgreSQL database (for example, using `pg_dump`) and your Kafka and Redis data volumes if you plan to migrate them.

PostgreSQL is upgraded from **16 to 17**. Although existing data volumes remain compatible, you must perform a proper database version upgrade (for example, using `pg_upgrade`) to ensure data integrity.

Kafka and Redis have been migrated from Bitnami images to official open-source alternatives. The new images use **different internal data directories and volume mappings**, so existing Bitnami volumes cannot be reused directly. You must either migrate your data manually or start from new, empty volumes.

<Aside type="caution">
  Starting from new volumes removes all data stored in Kafka and Redis, including client session states, undelivered messages, retained messages, and any other persisted broker data.
</Aside>

The standard upgrade script updates only the PostgreSQL database schema and does not include data migration logic for Kafka or Valkey.

<Aside type="note">
  <DocLink product={Products.TBMQ_PE} path="help">Contact us</DocLink> if you need guidance for the migration. We can offer general recommendations to help facilitate a smoother upgrade.
</Aside>
</ShowFor>

<ShowFor product={props.product} show={[Products.TBMQ]}>

<Aside type="tip">
  Interested in TBMQ PE? You can upgrade your TBMQ installation to <DocLink product={Products.TBMQ_PE} path="installation/upgrade-instructions">TBMQ PE</DocLink>.
</Aside>

<ConditionalHeading level={2} id="version-history" exclude="mqtt-broker-pe">Version history</ConditionalHeading>

Select a card above to open the upgrade guide for your deployment type, then follow its steps for the target version.
Each guide includes instructions for pulling the new image and running the upgrade script.

The table below lists all supported upgrade paths. Versions must be upgraded sequentially — you cannot skip versions.

| Target version | Upgrade from   | Notes                                                                                       |
| -------------- | -------------- | -------------------------------------------------------------------------------------------- |
| **2.3.0**      | 2.2.0          | See [third-party component updates](#third-party-component-updates-in-v230) below           |
| **2.2.0**      | 2.1.0          | See [Bitnami images notice](#notice-about-bitnami-images) below                             |
| **2.1.0**      | 2.0.0 or 2.0.1 | `fromVersion` is auto-detected — no longer required                                     |
| **2.0.1**      | 2.0.0          | `fromVersion`: `2.0.0`                                                                      |
| **2.0.0**      | 1.4.0          | `fromVersion`: `1.4.0` — see [migration note](#upgrading-to-200) below                  |
| **1.4.0**      | 1.3.0          | `fromVersion`: `1.3.0`                                                                      |
| **1.3.0**      | 1.2.1          | `fromVersion`: `1.2.1`                                                                      |
| **1.2.1**      | 1.2.0          | `fromVersion`: `1.2.0`                                                                      |
| **1.2.0**      | 1.1.0          | `fromVersion`: `1.1.0`                                                                      |
| **1.1.0**      | 1.0.0 or 1.0.1 | `fromVersion`: `1.0.0` or `1.0.1`                                                           |

<ConditionalHeading level={2} id="third-party-component-updates-in-v230" exclude="mqtt-broker-pe">Third-party component updates in v2.3.0</ConditionalHeading>

TBMQ v2.3.0 migrates all third-party components from Bitnami images to official open-source alternatives.
These changes improve performance, stability, and remove the dependency on Bitnami's image catalog.

These updates follow the modernization plan outlined in the <a href="https://github.com/thingsboard/thingsboard-ce-k8s/blob/master/BITNAMI-IMAGE-MIGRATION.md#3-long-term-production-solution" target="_blank" rel="noopener noreferrer">Bitnami Image Migration</a> guide.

| Component            | TBMQ v2.2.0                  | TBMQ v2.3.0         | Notes                                                                                       |
| -------------------- | ---------------------------- | ------------------- | ------------------------------------------------------------------------------------------- |
| **PostgreSQL**       | `postgres:16`                | `postgres:17`       | Improved performance and resource efficiency. Existing data volumes remain compatible.      |
| **Kafka**            | `bitnamilegacy/kafka:3.7.0`  | `apache/kafka:4.0.0` | Migrated from Bitnami to the official Apache Kafka image. New volume paths are required.   |
| **Redis → Valkey** | `bitnamilegacy/redis:7.2.5`  | `valkey/valkey:8.0` | Migrated from Bitnami Redis to Valkey. A new volume `tbmq-valkey-data` is introduced.       |

Before starting, create full backups of your environment — including the PostgreSQL database (for example, using `pg_dump`) and your Kafka and Redis data volumes if you plan to migrate them.

PostgreSQL is upgraded from **16 to 17**. Although existing data volumes remain compatible, you must perform a proper database version upgrade (for example, using `pg_upgrade`) to ensure data integrity.

Kafka and Redis have been migrated from Bitnami images to official open-source alternatives. The new images use **different internal data directories and volume mappings**, so existing Bitnami volumes cannot be reused directly. You must either migrate your data manually or start from new, empty volumes.

<Aside type="caution">
  Starting from new volumes removes all data stored in Kafka and Redis, including client session states, undelivered messages, retained messages, and any other persisted broker data.
</Aside>

The standard upgrade script updates only the PostgreSQL database schema and does not include data migration logic for Kafka or Valkey.

<Aside type="note">
  <DocLink product={props.product} path="help">Contact us</DocLink> if you need guidance for the migration. We can offer general recommendations to help facilitate a smoother upgrade.
</Aside>

<ConditionalHeading level={2} id="notice-about-bitnami-images" exclude="mqtt-broker-pe">Notice about Bitnami images (v2.2.0)</ConditionalHeading>

TBMQ deployments prior to v2.3.0 use **Bitnami images** for third-party components such as PostgreSQL, Redis, and Kafka.
The Bitnami team <a href="https://github.com/bitnami/charts/issues/35164" target="_blank" rel="noopener noreferrer">has announced important upcoming changes</a> to their catalog that affect these images.

- Starting **August 28, 2025**, the public `docker.io/bitnami` registry will only provide a small set of "latest" hardened images for development use.
- All versioned images will be moved to a temporary legacy registry: `docker.io/bitnamilegacy`. These images will remain available but will not receive updates or security patches.
- The long-term supported solution is Bitnami's new Secure Images (subscription-based).

**Recommended:** Complete the upgrade to v2.2.0 first (upgrades must be sequential), then continue to v2.3.0, which replaces all Bitnami images with official open-source alternatives. See [third-party component updates in v2.3.0](#third-party-component-updates-in-v230) for details.

If you are staying on v2.2.0 for now, update your image references from `docker.io/bitnami/...` to `docker.io/bitnamilegacy/...` to avoid disruption after August 28, 2025.

<ConditionalHeading level={2} id="upgrading-to-200" exclude="mqtt-broker-pe">Migration note for v2.0.0</ConditionalHeading>

Version 2.0.0 introduces significant improvements, including a migration from PostgreSQL to Redis for storing messages for persistent DEVICE clients.
For details, see the <DocLink product={props.product} path="architecture">architecture documentation</DocLink>.

<Aside type="caution">
  Back up your PostgreSQL database before upgrading to 2.0.0.
</Aside>
</ShowFor>
