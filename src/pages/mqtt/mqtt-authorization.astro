---
import MqttTopicLayout from '@components/MqttLearn/MqttTopicLayout.astro';
import MqttDiagram from '@components/MqttLearn/MqttDiagram.astro';
import type { FaqItem } from '@components/MqttLearn/FaqAccordion.astro';

const faq: FaqItem[] = [
	{
		q: 'What is MQTT authorization?',
		a: 'The broker’s enforcement of what an authenticated client may do — specifically which topics it may publish to and which it may subscribe from. It runs after authentication: being connected does not mean a client can use every topic.',
	},
	{
		q: 'What is an ACL in MQTT?',
		a: 'An access-control list: the set of rules that say which topics a client (or credential) is allowed to publish to or subscribe from. Brokers usually let you define publish rules and subscribe rules separately, often as topic patterns.',
	},
	{
		q: 'What is the difference between authentication and authorization?',
		a: 'Authentication verifies identity at connect. Authorization limits what that identity may do afterward, per topic. A client can authenticate successfully and still be denied a specific publish or subscribe.',
	},
	{
		q: 'What happens when a client violates an authorization rule?',
		a: 'The broker refuses that specific operation. A disallowed subscribe is rejected in the SUBACK; a disallowed publish is dropped and, in MQTT 5.0, acknowledged with a Not authorized reason code. The connection itself can stay open.',
	},
	{
		q: 'Can I use wildcards in authorization rules?',
		a: 'Yes. Rules are usually expressed as topic patterns, so one rule can cover a whole subtree — for example allowing a device to publish only under its own branch of the topic tree.',
	},
];
---

<MqttTopicLayout slug="mqtt-authorization" faq={faq}>
	<p>
		Authenticating a client answers <em>who are you</em>. <strong>Authorization</strong> answers <em
			>what are you allowed to do</em
		> — which <a href="/mqtt/topics/">topics</a> a client may publish to or subscribe from. It’s the third layer of <a
			href="/mqtt/security/">MQTT security</a
		>, and the one that contains the blast radius if a device is compromised.
	</p>

	<h2>How authorization works</h2>
	<p>
		Each client (or credential) has a set of access-control rules — an <strong>ACL</strong>. When the client tries to
		publish or subscribe, the broker checks the topic against those rules and allows or refuses the operation. Publish
		and subscribe are usually controlled separately, and rules are commonly written as topic patterns so one rule can
		cover a whole subtree.
	</p>

	<MqttDiagram caption="Authorization checks each publish against the client’s topic rules — allow or refuse">
		<svg
			viewBox="0 0 680 252"
			role="img"
			aria-label="MQTT authorization: with a publish rule of sensors/+/data, a publish to sensors/floor1/data is allowed while a publish to commands/reboot is refused with Not authorized"
		>
			<rect x="170" y="18" width="340" height="34" rx="8" style="fill:#f7f9fc;stroke:#dfe3ea"></rect>
			<text class="d-ctext d-mid" x="340" y="39" style="font-size:11px"
				>this client&apos;s publish rule: sensors/+/data</text
			>
			<rect class="d-actor" x="90" y="70" width="120" height="34" rx="9"></rect>
			<text class="d-alabel d-mid" x="150" y="92">Client</text>
			<rect class="d-actor d-actor--hero" x="450" y="70" width="120" height="34" rx="9"></rect>
			<text class="d-alabel d-alabel--hero d-mid" x="510" y="92">Broker</text>
			<line class="d-life" x1="150" y1="104" x2="150" y2="238"></line>
			<line class="d-life" x1="510" y1="104" x2="510" y2="238"></line>
			<line class="d-line" x1="150" y1="134" x2="507" y2="134" marker-end="url(#arGray)"></line>
			<rect class="d-chip" x="222" y="123" width="188" height="22" rx="11"></rect>
			<text class="d-ctext d-mid" x="316" y="138">PUBLISH sensors/floor1/data</text>
			<line class="d-line d-line--green" x1="510" y1="160" x2="153" y2="160" marker-end="url(#arGreen)"></line>
			<rect class="d-chip d-chip--green" x="250" y="149" width="132" height="22" rx="11"></rect>
			<text class="d-ctext d-ctext--green d-mid" x="316" y="164">PUBACK 0x00</text>
			<text class="d-yes" x="392" y="164" style="font-size:11px">✓ matches rule</text>
			<line class="d-line" x1="150" y1="196" x2="507" y2="196" marker-end="url(#arGray)"></line>
			<rect class="d-chip" x="222" y="185" width="188" height="22" rx="11"></rect>
			<text class="d-ctext d-mid" x="316" y="200">PUBLISH commands/reboot</text>
			<line class="d-line d-line--red" x1="510" y1="222" x2="153" y2="222" marker-end="url(#arRed)"></line>
			<rect class="d-chip" x="228" y="211" width="176" height="22" rx="11" style="stroke:#e3b3b3"></rect>
			<text class="d-ctext d-mid" x="316" y="226" style="fill:#c23b3b">PUBACK 0x87 Not authorized</text>
			<text class="d-no" x="410" y="226" style="font-size:11px">✗ no match</text>
		</svg>
	</MqttDiagram>

	<h2>Authorization in TBMQ</h2>
	<p>
		TBMQ authorizes publish and subscribe separately, per client credentials, using regular-expression topic patterns.
		The default rule allows all topics (<code>.*</code>), so hardening a deployment means tightening those patterns to
		scope each client to exactly the topics it needs — for example letting a device publish only under its own branch of
		the topic tree. See the <a href="/docs/mqtt-broker/security/overview/">security overview</a> for how authorization rules
		attach to client credentials.
	</p>
</MqttTopicLayout>
